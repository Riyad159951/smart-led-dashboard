# Copy this file to .env and fill in real values. Do NOT commit your .env file.
# Example usage:
#   1. cp .env.example .env
#   2. Edit .env and add your real secrets locally

# Adafruit IO key (never commit real key)
AIO_KEY=REPLACE_WITH_ADAFRUIT_IO_KEY

# Other env variables can go here
# API_BASE_URL=https://your-server.example.com